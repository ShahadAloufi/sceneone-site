{
  "name": "sceneone-site",
  "version": "1.0.0",
  "private": true,
  "description": "Scene One — serverless dependencies only (the frontend stays buildless).",
  "dependencies": {
    "@sparticuz/chromium": "^131.0.1",
    "puppeteer-core": "^23.9.0"
  }
}
